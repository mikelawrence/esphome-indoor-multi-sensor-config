substitutions:
  # name must be unique across all factory builds in this project and 24 characters or less
  name: "multi-sensor-pkga-none"
  friendly_name: "Multi-Sensor Pkg-A No-Radar"
  ref: "main"

packages:
  - !include common/common.yaml
  - !include common/pkga-sen6x.yaml
  - !include common/radar-none.yaml
  - !include common/add-co.yaml
  - !include common/add-ina2xx-rev-b.yaml
  - !include common/add-tsl2591.yaml
  - !include common/add-bmp581.yaml
  - !include common/add-microphone.yaml
  - !include common/feat-auto-vent.yaml
  - !include common/feat-smoke-alarm.yaml

dashboard_import:
  package_import_url: github://mikelawrence/esphome-indoor-multi-sensor-config/multi-sensor-pkga-none-esp32-s3.yaml@${ref}
  import_full_config: true

esphome:
  name: ${name}
  friendly_name: ${friendly_name}
  project:
    name: Mike Lawrence.${friendly_name}
    version: dev  # This will be replaced by the github workflows with the `release` version

api:

wifi:
  power_save_mode: none

web_server:
  port: 80
  version: 3
  auth:
    username: "sensor"
    password: "password"
    type: digest

ota:
  - platform: web_server
  - platform: http_request
    id: ota_http_request

http_request:

update:
  - platform: http_request
    id: update_http_request
    name: ${friendly_name} Firmware
    source: https://mikelawrence.github.io/esphome-indoor-multi-sensor-config/firmware/${name}.manifest.json

improv_serial:
