# Adds No Radar
# Not compatible with any other radar package
substitutions:
  radar: "None"

text_sensor:
  - platform: template
    name: "Radar Included"
    id: radar_included
    entity_category: diagnostic
    update_interval: 600s
    lambda: !lambda return {"${radar}"};
